import './App.css';

function App() {
//podemos crear el store con createStore, ojo con la versión de redux con la que trabaje porque ahora está desaconsejado.
//es mejor meter el store en el index dado que así tenemos más libertad sobre los componentes

  return (
<>

</>
  );
}

export default App;
